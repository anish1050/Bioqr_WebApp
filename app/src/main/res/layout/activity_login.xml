<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    tools:context=".LoginActivity"> <!-- android:background is handled by windowBackground now -->

    <!-- Logo Header -->
    <include
        android:id="@+id/includeHeaderLogo"
        layout="@layout/include_header_logo"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvLoginTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Secure Login"
        android:textAppearance="@style/TextAppearance.App.Headline1"
        app:layout_constraintTop_toBottomOf="@id/includeHeaderLogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <TextView
        android:id="@+id/tvLoginSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Access your BioQR account securely."
        android:textAppearance="@style/TextAppearance.App.BodyLarge"
        android:textColor="@color/bioqr_text_secondary"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/tvLoginTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Username/Email Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilUsernameEmail"
        style="@style/Widget.App.TextInputLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Username or Email"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/tvLoginSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:startIconDrawable="@drawable/ic_person_outline"
        app:startIconTint="@color/bioqr_text_muted">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etUsernameEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:textColor="@color/bioqr_text_primary"/>
    </com.google.android.material.textfield.TextInputLayout>

    <ImageView
        android:id="@+id/ivUsernameEmailStatus"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="12dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="@id/tilUsernameEmail"
        app:layout_constraintTop_toTopOf="@id/tilUsernameEmail"
        app:layout_constraintBottom_toBottomOf="@id/tilUsernameEmail"
        tools:src="@drawable/ic_check_circle"
        tools:visibility="visible"/>

    <TextView
        android:id="@+id/tvUsernameEmailStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="12sp"
        android:textColor="@color/bioqr_text_muted"
        app:layout_constraintTop_toBottomOf="@id/tilUsernameEmail"
        app:layout_constraintStart_toStartOf="@id/tilUsernameEmail"
        app:layout_constraintEnd_toEndOf="@id/tilUsernameEmail"
        tools:text="Valid username"/>

    <!-- Password Input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilPassword"
        style="@style/Widget.App.TextInputLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvUsernameEmailStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:passwordToggleEnabled="false"
        app:startIconDrawable="@drawable/ic_lock_outline"
        app:startIconTint="@color/bioqr_text_muted">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:textColor="@color/bioqr_text_primary"/>
    </com.google.android.material.textfield.TextInputLayout>

    <ImageView
        android:id="@+id/ivPasswordToggle"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="12dp"
        android:padding="2dp"
        android:src="@drawable/ic_eye_closed"
        android:contentDescription="Toggle password visibility"
        app:tint="@color/bioqr_text_muted"
        app:layout_constraintEnd_toEndOf="@id/tilPassword"
        app:layout_constraintTop_toTopOf="@id/tilPassword"
        app:layout_constraintBottom_toBottomOf="@id/tilPassword"/>

    <ImageView
        android:id="@+id/ivPasswordStatus"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@id/ivPasswordToggle"
        app:layout_constraintEnd_toStartOf="@id/ivPasswordToggle"
        app:layout_constraintTop_toTopOf="@id/tilPassword"
        app:layout_constraintBottom_toBottomOf="@id/tilPassword"
        tools:src="@drawable/ic_error_circle"
        tools:visibility="visible"/>

    <TextView
        android:id="@+id/tvPasswordStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="12sp"
        android:textColor="@color/bioqr_text_muted"
        app:layout_constraintTop_toBottomOf="@id/tilPassword"
        app:layout_constraintStart_toStartOf="@id/tilPassword"
        app:layout_constraintEnd_toEndOf="@id/tilPassword"
        tools:text="Password too short"/>

    <!-- Remember Me & Forgot Password -->
    <CheckBox
        android:id="@+id/cbRememberMe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Remember Me"
        android:textColor="@color/bioqr_text_secondary"
        android:buttonTint="@color/bioqr_primary"
        app:layout_constraintTop_toBottomOf="@id/tvPasswordStatus"
        app:layout_constraintStart_toStartOf="parent"/>

    <Button
        android:id="@+id/btnForgotPassword"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Forgot Password?"
        android:textAllCaps="false"
        android:textColor="@color/bioqr_accent"
        app:layout_constraintTop_toTopOf="@id/cbRememberMe"
        app:layout_constraintBottom_toBottomOf="@id/cbRememberMe"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Login Button -->
    <Button
        android:id="@+id/btnLogin"
        style="@style/Widget.App.Button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Login"
        app:layout_constraintTop_toBottomOf="@id/cbRememberMe"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ProgressBar
        android:id="@+id/pbLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:indeterminateTint="@color/bioqr_primary"
        app:layout_constraintTop_toTopOf="@id/btnLogin"
        app:layout_constraintBottom_toBottomOf="@id/btnLogin"
        app:layout_constraintStart_toStartOf="@id/btnLogin"
        app:layout_constraintEnd_toEndOf="@id/btnLogin"
        tools:visibility="visible"/>

    <!-- Go to Register -->
    <LinearLayout
        android:id="@+id/llGoToRegister"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:orientation="horizontal"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Don't have an account? "
            android:textColor="@color/bioqr_text_secondary"
            android:textSize="14sp"/>

        <Button
            android:id="@+id/btnGoToRegister"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Register Now"
            android:textAllCaps="false"
            android:textColor="@color/bioqr_accent"
            android:textSize="14sp"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:minHeight="0dp"
            android:minWidth="0dp"/>
    </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
